first.className = "yellow text-dark"
first.classList //to check the class list on browser console
first.classList.remove("red")
first.classList.add("red")
first.classList.toggle("red") //if red is present then remove else add
first.classList.contains("yellow")